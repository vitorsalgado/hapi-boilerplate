/* eslint-disable global-require */

'use strict';

describe('Config', () => {
	it('should crash when config is invalid according to schema', () => {
		// const load = () => require('./');
		// expect(load).toThrow();
	});
});
